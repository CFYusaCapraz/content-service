spring:
  datasource:
    username: sa
    password: password
    url: jdbc:h2:mem:content
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  liquibase:
    enabled: false
